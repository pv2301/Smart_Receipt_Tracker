# Flutter SDK clonado localmente (não necessário pela Lambda)
flutter/
flutter_sdk/

# Código-fonte Flutter (o output está em frontend/build/web)
frontend/

# Docker
docker-compose.yml

# requirements.txt raiz (Lambda usa api/requirements.txt)
requirements.txt
