# Flutter SDK clonado localmente (não necessário no Vercel — o build clona em /tmp)
flutter/
flutter_sdk/

# Docker
docker-compose.yml

# requirements.txt raiz (Lambda usa api/requirements.txt)
# Âncora /: exclui SOMENTE a raiz, não api/requirements.txt nem backend/requirements.txt
/requirements.txt
